import styled from "styled-components";

export const StyledInput = styled.input`
    background: var(--green4);
    height: 40px;
    width: 90%;

    border-radius: 6px;

    ::placeholder {
        color: var(--green5);
        text-align: center;
    }
`;
